<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.accounting.mapper.UserMapper">

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultType="com.example.accounting.entity.User">
        SELECT * FROM user
        WHERE username = #{username}
          AND deleted = 0
        LIMIT 1
    </select>

    <!-- 查询用户角色代码列表 -->
    <select id="selectRoleCodesByUserId" resultType="string">
        SELECT r.code FROM role r
        INNER JOIN user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.deleted = 0
          AND ur.deleted = 0
    </select>

</mapper>